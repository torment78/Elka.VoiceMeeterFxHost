<Window x:Class="Elka.VoiceMeeterFxHost.App.SinglePingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Single Ping"
        Icon="Assets/VoicemeeterDelay.ico"
        Width="560"
        Height="380"
        MinWidth="520"
        MinHeight="340"
        Background="{StaticResource WindowBackgroundBrush}"
        FontFamily="Segoe UI"
        FontSize="14"
        WindowStartupLocation="CenterOwner">
  <Window.Resources>
    <Style x:Key="PingComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
      <Setter Property="Background" Value="{StaticResource FieldBrush}" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ComboBoxItem}">
            <Border x:Name="ItemChrome"
                    Background="{TemplateBinding Background}"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemChrome" Property="Background" Value="{StaticResource RouteActiveBrush}" />
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="ItemChrome" Property="Background" Value="#184F57" />
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid Margin="18">
    <Border Style="{StaticResource PanelCard}"
            BorderBrush="{StaticResource DelayAccentBrush}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="138" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Margin="0,0,0,16">
          <TextBlock Text="Single Ping"
                     FontSize="21"
                     FontWeight="SemiBold"
                     Foreground="{StaticResource DelayAccentBrush}" />
          <TextBlock Text="Send a Windows audio pulse into VoiceMeeter and listen back from a Windows capture endpoint."
                     Margin="0,4,0,0"
                     Style="{StaticResource MutedText}" />
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Text="Send device"
                   FontWeight="SemiBold"
                   VerticalAlignment="Center"
                   Margin="0,0,14,10" />
        <ComboBox x:Name="PingInputComboBox"
                  Grid.Row="1"
                  Grid.Column="1"
                  Margin="0,0,0,10"
                  DisplayMemberPath="DisplayName"
                  ItemContainerStyle="{StaticResource PingComboBoxItemStyle}"
                  SelectionChanged="EndpointComboBox_SelectionChanged" />

        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Text="Listen device"
                   FontWeight="SemiBold"
                   VerticalAlignment="Top"
                   Margin="0,8,14,0" />
        <StackPanel Grid.Row="2"
                    Grid.Column="1">
          <ComboBox x:Name="ReturnOutputComboBox"
                    Margin="0,0,0,12"
                    DisplayMemberPath="DisplayName"
                    ItemContainerStyle="{StaticResource PingComboBoxItemStyle}"
                    SelectionChanged="EndpointComboBox_SelectionChanged" />

          <Border Background="{StaticResource FieldBrush}"
                  BorderBrush="{StaticResource SubtleBorderBrush}"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="12">
            <TextBlock x:Name="ResultTextBlock"
                       Text="Ready. No ping has been sent."
                       Foreground="{StaticResource TextBrush}"
                       TextWrapping="Wrap" />
          </Border>
        </StackPanel>

        <Grid Grid.Row="3"
              Grid.ColumnSpan="2"
              Margin="0,16,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <TextBlock x:Name="SelectionTextBlock"
                     Grid.Column="0"
                     VerticalAlignment="Center"
                     Style="{StaticResource MutedText}" />
          <Button x:Name="PingButton"
                  Grid.Column="1"
                  Content="Ping"
                  Width="90"
                  Style="{StaticResource RouteButton}"
                  Click="PingButton_Click" />
          <Button Grid.Column="2"
                  Content="Close"
                  Width="90"
                  Margin="8,0,0,0"
                  Click="CloseButton_Click" />
        </Grid>
      </Grid>
    </Border>
  </Grid>
</Window>
